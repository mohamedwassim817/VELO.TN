{% extends 'layout.html.twig' %}

{% block body %}
    <!-- Stylesheet -->
    <link rel="stylesheet" href="{{ asset('assets/css/elisyam-1.5.css') }}">
    <h1>Modifier Sous_categorie </h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Modifier" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('sous_categorie_index') }}">retour à la liste </a>
        </li>
        <li>
            <a href="{{ path('rmSc', { 'id': sous_categorie.id }) }}" > Supprimer</a>
        </li>
    </ul>
{% endblock %}
