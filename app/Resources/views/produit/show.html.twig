{% extends 'layout.html.twig' %}

{% block body %}
    <div class="container">
    <h1>Produit</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ produit.nom }}</td>
            </tr>
            <tr>
                <th>Modele</th>
                <td>{{ produit.modele }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ produit.type }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit.prix }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td> <img class="card-img" src="{{ asset('uploads/Products/'~produit.image) }}" style="height: 110px;width: 180px;" alt="Vans"></td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ produit.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('produit_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('produit_edit', { 'id': produit.id }) }}">Modifier</a>
        </li>
        <li>
            <a href="{{ path('rmp', { 'id': produit.id }) }} "> Supprimer</a>
        </li>
    </ul>
    </div>
{% endblock %}
